name: neo4j/docs-maven-plugin ingest

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch: {}

jobs:
  ingest:
    runs-on: ubuntu-latest
    name: Ingest
    steps:
      - name: Checkout self
        uses: actions/checkout@v2
        with:
          path: main
      - name: Checkout target
        uses: actions/checkout@v2
        with:
          repository: neo4j/docs-maven-plugin
          path: target
      - name: Ingest
        uses: ./main
        id: ingest
        with:
          gcp-service-account-key: ${{ secrets.GCR_KEY }}
