name: 'Ingest'
description: 'Ingest the thing'
inputs:
  style-name:
    description: 'Style name'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.project-dir }}