name: 'Ingest'
description: 'Ingest the thing'
inputs:
  style-name:
    description: 'Style name'
    required: false
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    GCR_KEY: ${{ secrets.GCR_KEY }}
  args:
    - ${{ inputs.project-dir }}