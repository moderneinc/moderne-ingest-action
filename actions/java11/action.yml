name: 'Java 11 Ingest'
description: 'Ingest using openjdk 11'
inputs:
  styleName:
    description: 'Style name'
    required: false
  gcpKey:
    description: 'GCP service account key'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
