spring:
  application.name: ingest
  profiles.default: dev
logging:
  level:
    ROOT: debug
---
spring:
  config.activate.on-profile: '!prod'
  cloud:
    gcp:
      core.enabled: false
      storage.enabled: false
